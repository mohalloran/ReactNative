export const renderListings = (listings) => {
         console.log('In Utils Rendering listings....');

         listings.forEach(function (listing) {
              console.log('listing price is:', listing.price);
        });
};




